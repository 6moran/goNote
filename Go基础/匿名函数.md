### 匿名函数的运用：

(匿名函数：只用一次的函数）

1.常用用法：

```go
//匿名函数
result := func(num1 int, num2 int) int {
    return num1 + num2
}(10, 20)
fmt.Println(result)
```



2.将匿名函数赋值给一个变量：

```go
sub := func(num1 int, num2 int) int {
    return num1 - num2
}
//调用sub
result2 := sub(30, 70)
fmt.Println(result2)
```



