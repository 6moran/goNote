**方法1:**

```go
var count int
fmt.Println("请输入数量:")
fmt.Scanln(&count)	//传的是变量的地址
```

**方法2：**

```go
scanner := bufio.NewScanner(os.Stdin)	// 从标准输入读取一整行（包含空格）
scanner.Scan() //单独写一行表示忽略它的返回值bool类型
text := scanner.Text()	
```

*需要导bufio和os包*

